import { css } from 'lit-element';

export const lightTheme = css`
  --primary-color: var(--pure-white);
  --secondary-color: var(--mine-shaft);
  --bold-accent-color: var(--sandal);
  --regular-accent-color: var(--bone);
  --light-accent-color: var(--dawn-pink);
`;
